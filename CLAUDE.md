# CLAUDE.md

always answer in summary in less than 10 lines

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Nature du dépôt
Ce dépôt n'est **pas un projet applicatif** : c'est une **collection de plugins Claude Code** formant une "Factory IA" (fabrique logicielle à forte assistance IA). Les livrables sont des **skills Markdown** + un `plugin.json` par plugin. **Ni build, ni lint, ni tests** - la seule "compilation" est la validité JSON des manifestes.

- Dépôt : `github.com/NASSWIEL/Factory-IA` (**privé depuis 2026-06-26**), branche `main`. Documents de conception dans le parent (`../context-complet.txt`, `../architecture.md`).
- Distribution : marketplace **`git-subdir`** (`.claude-plugin/marketplace.json`) -> **Shapsha-Factory**. Le repo pro CGI (`CGI-Shapsha-Factory/CGI-Factory-AI`) reçoit les évolutions par **fast-forward direct sur `cgi/main`** (commit -> branche `optimisation` -> `git push cgi optimisation` -> `git push cgi optimisation:main`, jamais de force-push). **Poussé par le compte `NASSWIEL`** (owner de l'org ; l'ancien compte `asswiel` a été retiré de l'org le 2026-07-06). Détails (remotes, token, identité) en **mémoire projet interne**, pas dans l'arbre versionné. Côté utilisateur : `/plugin marketplace update Shapsha-Factory`.
- Installation : **uniquement via la marketplace** (`.claude-plugin/marketplace.json` -> Shapsha-Factory). Côté utilisateur : `/plugin marketplace add` puis `/plugin install <plugin>@Shapsha-Factory`. La marketplace expose aussi `category`/`tags` par plugin (groupement par rôle dans `/plugin` Discover). *(L'ancien installeur façon BMAD - `install.py` / wrapper npx / `INSTALL.md` - a été retiré : la marketplace suffit.)*
- Git : branche `main`, email `naifsaleem20@gmail.com`, compte GitHub `NASSWIEL` (origin **et** cgi). Commit/push uniquement sur demande.

## Les plugins (4 contrats amont + 2 aval + 1 transversal)
| Dossier | Rôle | État | Détail |
|---------|------|------|--------|
| `cadrage/` | Contrat **fonctionnel** (captation -> pack repris par l'architecte) | **construit** | voir `cadrage/CLAUDE.md` |
| `architecte/` | Contrat **technique** (drivers, attributs qualité, composants, stack, ADR, walking skeleton, conventions/linters, diagrammes, **enforcement tests + formatage** ; protection de branche gérée côté GitHub, pas de hook local) | **construit** | voir `architecte/CLAUDE.md` |
| `designer/` | Contrat **design** - **atelier de couverture** (checklist fondation/expérience/technique pré-remplie par les handoffs) qui produit le **prompt Claude Design** + rapport de couverture + handoff ; le design system naît dans **Claude Design**, son export est committé dans `designer-out/maquette-de-claude-design/`, pas généré par le plugin | **construit** | voir `designer/CLAUDE.md` |
| `assembleur/` | Convergence des 3 contrats par feature + **tickets Linear (`premier-alimente-linear` : 1 ticket `Feature`/feature + 1 sous-ticket `Task`/Functional Requirement, tout en Backlog, label `Feature` seul ; `creation-tasks-linear` : 1 sous-ticket `Task`/phase de `tasks.md` après `/speckit.tasks` ; MAJ d'état via `update-issue-linear` ; via MCP linear-prism)** + **`create-cowork-md`** (contexte de supervision `init-cowork.md` à la racine pour le PO/Quark : liens GitHub + Linear) + amorçage repo SpecKit (constitution, CLAUDE.md, briefs 3-faces, glossaire consolidé, MEMORY.md, seeds spec.md) ; **n'écrit pas dans le repo cible** (4 exceptions bornées : Linear externe, `specify init`, `init-cowork.md` à la racine, et `CLAUDE.md` + `memory/` déployés dans le `.claude/` du projet) | **construit** | voir `assembleur/CLAUDE.md` |
| `validation/` | Phase **aval** (recette fonctionnelle d'une feature livrée, **avant** le traitement des écarts) - **plan de test dérivé de la spec** (`specs/<feature>/spec.md` : 1 cas `TC-<feature>-NNN` par critère d'acceptation, tracé, critère non testable = `A CLARIFIER`, jamais interprété) + **exécution navigateur** contre l'environnement de recette (extension Chrome "Claude in Chrome" prioritaire, MCP Playwright en repli, ou mission différée `mission-cowork.md` pour Claude Cowork - contrat de résultats commun, **déroulé effectif** de chaque cas en langage naturel) + **rapport de recette tracé** exigence par exigence avec **porte de recette humaine** (verdict du testeur dans le rapport committé + commentaire Linear, jamais dans le manifeste). **Détecte et trace ; le traitement passe par `maintenance/`** (renvois vers `creation-anomalie`/`creation-evolution`, contenu pré-rempli, jamais de création directe) | **construit** | voir `validation/CLAUDE.md` |
| `maintenance/` | Phase **aval** (après la livraison d'une feature) - **anomalies et évolutions de maintenance** : le PO crée dans Linear (sous-ticket du ticket `Feature`, labels `Anomalie`/`Evolution`, gabarits complets exigés), le développeur réalise en orchestrant SpecKit (`correction-anomalie` : tri évolution déguisée -> statut "Requalifiée en évolution", enquête code, clôture refusée sans spec/tâches/Linear à jour ; `realisation-evolution` : spec d'abord -> `/speckit.clarify` -> plan validé -> `/speckit.implement` **cadré** -> non-régression). **Aucun état d'avancement dans le manifeste** (tout vit dans Linear) ; pas de `maintenance-out/` | **construit** | voir `maintenance/CLAUDE.md` |
| `couts/` | **Transversal** (pas une phase) - mesure du **coût de simulation** **à la fin de session** (sans latence par tour) : **tableau par session** (tokens input/output + coût €) ; hook `SessionEnd` **ancré dans le dossier courant** (un enregistrement par message, 5 catégories, dédup globale par clé). Simulation seule (pas de coût réel ni de config) | **construit** | voir `couts/CLAUDE.md` |

Chaque plugin a (ou aura) son propre **`<plugin>/CLAUDE.md`** détaillé. Pour travailler sur `cadrage`, lire **`cadrage/CLAUDE.md`** en premier.

**Vue d'ensemble utilisateur** de toute la chaîne (4 phases amont + validation + maintenance, ordre des skills, portes humaines) : le skill **`/cadrage:help-factory`** - l'**aide unique** (statique, un tableau par plugin).

## Principe transverse
- **L'IA propose, l'humain tranche.** Aucune gate structurante n'est franchie par l'IA.
- **Figer les contrats en amont**, automatiser l'hygiène en aval par des hooks déterministes (socle à venir).
- Les sorties de cadrage + architecte + designer convergent dans l'**assembleur** ; il produit un **paquet de handoff** dans `assembleur-out/` - dont une **pré-constitution** (au format `constitution.md`, dérivée des 3 contrats) et des **graines spec** par feature - que **l'équipe** donne à SpecKit. L'assembleur **n'écrit jamais** dans le repo cible. Il n'y a plus de skill handoff côté cadrage.

## Conventions communes
- Skills : `<plugin>/skills/<nom>/SKILL.md`, frontmatter `name:` + `description:` (une phrase courte anglaise). Corps des skills en **français** (instructions).
- Langue : **tout en français** (skills, templates, artefacts, interaction) ; seuls les identifiants/valeurs machine du manifeste et noms d'outils/formats restent tels quels.
- **Typographie humaine** (anti "style IA") : rien de ce que la Factory produit (artefacts `-out/`, prompts, rapports, sorties chat) n'emploie de tiret cadratin (em dash), de points de suspension unicode, de flèches unicode, de guillemets à chevrons, ni de coche/croix. On utilise les équivalents clavier (tiret simple, `...`, `->`, guillemets droits, `Oui`/`Non`). Règle détaillée dans la section Typographie de chaque `<plugin>/references/ux-conventions.md`.
- Workspace runtime d'un projet client : le **manifeste committé** `manifest.json` (contrat machine partagé, **versionné**, voyage avec le repo) + `.factory/<plugin>/` pour les **gabarits** (mécanique cachée, **git-ignorée**, régénérable) + un dossier de sortie par plugin à la racine (`cadrage-out/`, `architecte-out/`, `designer-out/`, `assembleur-out/`, `validation-out/<feature>/`). Prompts Claude Design : `cadrage-out/prompts/` (cadrage) et `designer-out/prompts/` (designer - consolidé sous son `-out/`), en fichiers plats. Le designer reçoit aussi l'export du design system dans `designer-out/maquette-de-claude-design/`.
- **Handoff = `-out/` committés (exécution indépendante).** Chaque phase peut être exécutée par une autre personne, sur une autre machine, dans une autre session : elle ne s'appuie **que** sur les dossiers `-out/` committés de la phase précédente, jamais sur l'historique de chat. **Le manifeste est committé** dans `manifest.json` : il **voyage avec le repo**, donc les **verdicts de porte** et l'état de découverte **sont transportés** vers la phase suivante / une autre machine (fin de la contradiction "contrat vs ignoré"). **`.factory/` reste local et git-ignoré**, mais **seulement pour les gabarits** (`.factory/<plugin>/`) **et le journal des coûts** (`.factory/couts/`) - régénérables via les `-init`, **plus le manifeste**. Les garde-fous ne doivent échouer que sur un **fichier réellement manquant**, en le nommant.
- **Bootstrap `.factory/` par chaque `-init` + ownership du `.gitignore`.** Comme `.factory/` est git-ignoré et absent d'un clone frais, **chaque `-init`** (cadrage, architecte, designer, assembleur) **repose ses gabarits dans `.factory/<plugin>/`** (git-ignorés) **et son bloc de manifeste dans `manifest.json`** (committé, créé s'il manque) - aucun ne présuppose un `.factory/` préexistant. Le **`.gitignore`, lui, est committé** (il voyage avec le repo) : sa **première version est générée par le cadrage** (`cadrage-init`) ; **toutes les phases aval ne font que le compléter** - elles **ajoutent** les lignes manquantes (`.factory/`, puis `.env`/`.env.*.local` côté architecte), sans jamais le **réécrire ni l'écraser**, et ne le **créent que s'il est absent** (clone où le cadrage n'a pas tourné dans ce dossier).
- **Identité des features (clé de jointure inter-plugins)** : le **use case** du cadrage (ex. `UC2`) est l'identifiant fonctionnel **stable**. L'architecte **propose** la séquence `architecture.feature_sequence` = objets `{id, ucs, name}` où `ucs` est une **liste** (l'`id` `001...` = ordre de fabrication ; une feature peut **bundler plusieurs use cases** en cas de fusion ; **aucune notion de MVP** nulle part - l'ordre est purement technique) ; l'**assembleur** en a l'**autorité finale** : il arbitre le découpage (découper/fusionner), **réécrit** `architecture.feature_sequence`, et le registre est **figé à l'init Linear** (`premier-alimente-linear`) - immuable ensuite. L'assembleur joint les faces **fonctionnelle** (briefs cadrage) et **technique** (composants/ADR architecte) **par use case**. La **face design est globale** (pas de jointure par feature) : le designer produit un **handoff** (réf. de l'export du design system committé dans `designer-out/maquette-de-claude-design/` + guidelines) appliqué à tous les écrans. Composants : services **+ application front-end** = `composants.md` (architecture technique, architecte) ; le **design system visuel** naît dans Claude Design et son export est committé dans `designer-out/maquette-de-claude-design/`.
- Marketplace : ajouter un plugin = une entrée `git-subdir` dans le `marketplace.json` de la marketplace Shapsha-Factory (`url` = ce dépôt Factory-IA, `path` = dossier du plugin).

## Flux d'artefacts (contrat I/O explicite, déterministe)
Le **manifeste** (contrat machine partagé) est **committé** dans `manifest.json`, créé par `cadrage-init` ; les **gabarits** vivent dans `.factory/<plugin>/` (caché, **git-ignoré**, régénérable). Les **documents générés** vont dans un dossier de sortie **par plugin** à la racine : `cadrage-out/`, `architecte-out/`, `designer-out/`, `assembleur-out/`, `validation-out/<feature>/` (+ `<plugin>-out/prompts/` pour les prompts, en **fichiers plats** `<NNN>-<JJ-MM>-<nom>.md`). Le **manifeste** est le contrat machine ; chaque plugin **écrit dans son `-out/`** et **lit directement** ceux de l'amont (pas d'hypothèse implicite). Contrat de consommation strict : **architecte ⊂ cadrage** ; **designer ⊂ cadrage + architecte** ; **assembleur ⊂ les trois**. **Plus de skill handoff** côté cadrage : la convergence est le rôle de l'**assembleur**, qui produit un **paquet** (il n'écrit jamais dans le repo cible).

| Plugin | Consomme (entrées) | Produit (sorties) | Bloc manifeste |
|---|---|---|---|
| **cadrage** | matière brute (transcripts/docs), déposée dans `cadrage-out/source-contexte/` (facultatif) | `cadrage-out/`: capture-brute, project-frame, product-brief, glossaire, spec-index, coupling-map, completude-report, `features-fonctionnels-brief/<feature>.md` ; `cadrage-out/prompts/` | `artifacts`, `demonstrateur`, `discovery`, `definition_of_ready` |
| **architecte** | `cadrage-out/`: product-brief, glossaire, spec-index, project-frame, briefs | `architecte-out/`: facteurs-et-qualite, composants, stack-technique, standards-ingenierie, diagrammes (+ `diagrammes/*.png`), risques, **impact-design**, `decisions/ADR-*.md` ; `conventions/` + `.env*`/`.claude/`/`lefthook.yml`/`.gitignore` (racine, optionnels) | `architecture` |
| **designer** | `cadrage-out/` (product-brief, glossaire, spec-index, démonstrateur) **+** `architecte-out/impact-design.md` | `designer-out/`: coverage-report, design-guidelines, `prompts/<NNN>-<JJ-MM>-claude-design.md`, `maquette-de-claude-design/` (export committé) | `design` |
| **assembleur** | `cadrage-out/` + `architecte-out/` (stack-technique, components, standards, decisions/, facteurs-et-qualite) + `designer-out/` (design-guidelines, coverage-report) | `assembleur-out/` (paquet de handoff) : `pre-constitution.md`, `features/<id>-*.md`, `feature-map.md`, `technical-context.md`, `coherence-report.md`, `attack-plan.md` ; **+ `.claude/CLAUDE.md` et `.claude/memory/{MEMORY,domain,architecture,design,features}.md`** écrits directement dans le `.claude/` du projet (déploiement, actifs sans copie manuelle) ; **+ tickets Linear** (`premier-alimente-linear` : 1 ticket `Feature`/feature + 1 sous-ticket `Task`/Functional Requirement (`fr`), tout en Backlog, label `Feature` seul ; `creation-tasks-linear` : 1 sous-ticket `Task`/phase de `specs/<feature>/tasks.md` - labels résolus par nom, `parentId` sur le ticket `Feature`, **créés dans Linear** (idempotence via `parentId` + jeton `Phase N -`) : les sous-tickets de phase **et** l'état d'avancement vivent **dans Linear**, jamais dans le manifeste committé (concurrence multi-dev) ; MAJ d'état via `update-issue-linear`) ; **+ `init-cowork.md`** à la racine (create-cowork-md : contexte de supervision PO/Quark, liens GitHub + Linear) ; **+ `specify init`** dans le repo (install-speckit) | `assembly`, `linear`, `cowork`, `speckit` |
| **validation** (aval, recette fonctionnelle) | `specs/<feature>/spec.md` (scénarios d'acceptation Given/When/Then, FR-xxx, SC-xxx, edge cases - **lecture seule**) + `assembleur-out/feature-map.md` + tickets Linear (bloc `linear` : ticket `Feature` de la feature) + environnement de recette déployé | `validation-out/<feature>/` (committé) : `plan-de-test.md` (1 cas/critère, tracé), `mission-cowork.md` (voie Cowork), `resultats/execution-<JJ-MM>.md` (+ `resultats/preuves/*.png` - un fichier par exécution, jamais écrasé), `rapport-de-recette.md` (matrice critère -> cas -> verdict -> preuve -> décision + **Verdict de recette** humain) ; **+ commentaire Linear** sur le ticket `Feature` (verdict ; statut sur confirmation) ; renvois vers `/maintenance:*` pour créer les tickets d'écart | `validation` (config statique : environnement de recette, outil habituel - **jamais de verdict ni d'avancement**) |
| **maintenance** (aval, post-livraison) | tickets Linear (bloc `linear` : tickets `Feature` = parents) + `specs/<feature>/` (spec, plan, tasks) + `assembleur-out/feature-map.md` + `architecte-out/composants.md` + `.specify/memory/constitution.md` | tickets/commentaires/statuts **dans Linear** (anomalies label `Anomalie`, évolutions label `Evolution`, statut "Requalifiée en évolution") + mises à jour de `specs/<feature>/` **via les commandes SpecKit** (spec = écart seul ; plan/tâches régénérés, jamais édités à la main) ; **aucun artefact committé propre** (pas de `maintenance-out/`) | `maintenance` (config statique : team, labels, statut de requalification - **jamais d'état d'avancement**) |
| **couts** (transversal) | transcript complet à la fin de session (hook `SessionEnd`) + `.factory/couts/price-table.json` | `.factory/couts/<aaaa-mm>/<session-id>.jsonl` (**un enregistrement/message**, réécrit à chaque fin de session) ; `.factory/couts/rapport-couts.md` (**tableau par session** : tokens input/output + coût €, **versionné** : `rapport-couts.md`, `-2`, `-3`... jamais écrasé) ; hook `SessionEnd` dans `.claude/` du **dossier courant** (compteur ancré, ne mesure que ce dossier). **Tout `.factory/` git-ignoré** (dont `.factory/couts/`). **Simulation seule** (pas de coût réel/config). | `costs` |

**Enforcement** (3 couches, déjà en place) : (1) **portes d'entrée** de chaque `-init` (refusent si la phase amont n'est pas validée + vérifient la présence des fichiers attendus dans les `-out/` amont) ; (2) **garde-fous déterministes** `check_discovery`/`check_ready`/`check_architecture`/`check_design`/`check_assembly`/`check_linear`/`check_validation`/`check_maintenance` (défaut `manifest.json`) ; (3) **manifeste** (jointure par use case). **Frontière SpecKit** : l'assembleur produit un **paquet** dans `assembleur-out/` (pré-constitution, graines spec, carte des features, contexte technique, CLAUDE.md, mémoire) ; **c'est l'équipe** qui lance `specify init` puis `/speckit.constitution` (depuis `pre-constitution.md`) et les `/speckit.specify`. L'assembleur **n'écrit jamais** `.specify/`, `specs/NNN/spec.md`, ni aucun fichier que SpecKit génère.
