// Fichier COMMITTÉ et INCLUS dans le bundle client = PUBLIC.
// N'y mettez JAMAIS de secret - les secrets restent côté serveur
// (proxy / gestionnaire de secrets). Ici, uniquement des URLs et des flags publics.
export const environment = {
  production: true,
  apiUrl: 'https://api.example.com'
};
